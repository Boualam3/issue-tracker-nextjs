/** @type {import('next').NextConfig} */
const nextConfig = {
  // async headers() {
  //   return [
  //     {
  //       // in this pattern we represent all endpoints in our application
  //       source: "/:path*",
  //       headers: [{ key: "referrer-policy", value: "no-referrer" }],
  //     },
  //   ]
  // },
}

module.exports = nextConfig
